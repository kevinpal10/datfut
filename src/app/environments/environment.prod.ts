// Configuración de producción. `angular.json` sustituye `environment.ts` por
// este archivo en la configuración `production` (`fileReplacements`).
//
// Cambia `apiUrl` por el host real del backend antes de desplegar.
export const environment = {
  production: true,
  apiUrl: 'https://back-datfut.onrender.com/health'
};
